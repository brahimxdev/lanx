import { Router } from "express";

export const router = Router();

// api/v1/users

// Route for getting current user (need auth access)
// router.get("/me");

// // Route for deleting current user account (need auth access)
// router.delete("/me");
