info:
  name: change password
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{baseUrl}}/account/password"
  body:
    type: json
    data: |-
      {
        "existingPassword": "neWass3!!!",
        "newPassword": "freshPass234!!!"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
