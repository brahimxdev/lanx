info:
  name: Delete User
  type: http
  seq: 15

http:
  method: DELETE
  url: "{{baseUrl}}/account"
  body:
    type: json
    data: |-
      {
        "currentPassword": "DevPassword123!"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
