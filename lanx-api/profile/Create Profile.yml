info:
  name: Create Profile
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/profile"
  body:
    type: json
    data: |-
      {
        "firstName": "lekxy", 
        "lastName": "ola", 
        "businessName": "lek inc",
        "professionId": "087abab0-95c3-4b3e-97e3-92bac56b4a24",
        "countryCode": "US",
        "currencyCode": "USD"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
