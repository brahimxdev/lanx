info:
  name: Get Profile
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/profile"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
