info:
  name: get profile
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/profile"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
