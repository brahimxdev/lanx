info:
  name: Upload Logo
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/profile/logo"
  body:
    type: multipart-form
    data:
      - name: logo
        type: text
        value: image file
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
