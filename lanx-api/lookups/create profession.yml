info:
  name: create profession
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/lookup/professions"
  body:
    type: json
    data: |-
      {
        "name": "API Developer"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
