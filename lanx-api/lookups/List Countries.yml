info:
  name: List Countries
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/lookup/countries?search=canada"
  params:
    - name: search
      value: canada
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
