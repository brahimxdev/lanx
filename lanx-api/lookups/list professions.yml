info:
  name: List Professions
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/lookup/professions?search=web"
  params:
    - name: search
      value: web
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
