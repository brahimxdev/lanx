info:
  name: list professions
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/lookup/professions?search=di"
  params:
    - name: search
      value: di
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
