info:
  name: list countries
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/lookup/countries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
