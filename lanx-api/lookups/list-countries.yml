info:
  name: list countries
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/lookup/countries?search=unit"
  params:
    - name: search
      value: unit
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
