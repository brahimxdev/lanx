info:
  name: Archive Client
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/clients/{{clientId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
