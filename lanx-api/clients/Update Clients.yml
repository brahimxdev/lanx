info:
  name: Update Clients
  type: http
  seq: 4

http:
  method: PATCH
  url: /{{baseUrl}}/clients
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
