info:
  name: Update Client
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/clients"
  body:
    type: json
    data: |-
      {
      //   "firstName": "Jane",
      //   "lastName": "Daniel", 
        "email": "jane@email.com", 
        "companyName": "Jane Inc",
        "notes": "Working on creating Cloud services for them"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
