info:
  name: Get Client
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/clients/{{clientId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
