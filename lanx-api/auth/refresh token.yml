info:
  name: refresh token
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseUrl}}/auth/refresh-token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
