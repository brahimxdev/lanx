info:
  name: reset password
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/auth/reset-password"
  body:
    type: json
    data: |-
      {
        "email": "ibrahim.techresources@proton.me",
        "confirmationCode": "272685",
        "newPassword": "neWass3!!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
