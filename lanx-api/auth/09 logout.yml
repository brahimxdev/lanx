info:
  name: 09 logout
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/logout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
