info:
  name: delete user
  type: http
  seq: 15

http:
  method: DELETE
  url: "{{baseUrl}}/account"
  body:
    type: json
    data: |-
      {
        "currentPassword": "IBRahim123!!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
