info:
  name: 12 refresh token
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/refresh-token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
