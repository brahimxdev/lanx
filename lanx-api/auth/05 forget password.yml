info:
  name: 05 forget password
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/forget-password
  body:
    type: json
    data: |-
      {
        "email": "ibrahim@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
