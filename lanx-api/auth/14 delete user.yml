info:
  name: 14 delete user
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{baseUrl}}/users/me"
  body:
    type: json
    data: |-
      {
        "currentPassword": "IBRahim123!!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
