info:
  name: 02 confirm email
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/sign-up
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
