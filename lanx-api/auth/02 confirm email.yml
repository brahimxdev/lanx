info:
  name: 02 confirm email
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/auth/confirm-email"
  body:
    type: json
    data: |-
      {
        "email": "ibrahim.techresources@proton.me",
        "confirmationCode": "34456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
