info:
  name: 07 change password
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:3000/api/v1/auth/change-password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
