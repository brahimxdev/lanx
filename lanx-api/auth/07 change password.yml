info:
  name: 07 change password
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/auth/change-password"
  body:
    type: json
    data: |-
      {
        "existingPassword": "IBRahim123!!!",
        "newPassword": "randomPass222"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
