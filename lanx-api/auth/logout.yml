info:
  name: logout
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/account/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
