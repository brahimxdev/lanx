info:
  name: 06 reset password
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/reset-password
  body:
    type: json
    data: |-
      {
        "email": "ibrahim@gmail.com",
        "confirmationCode": "123456",
        "newPassword": "randomPass3!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
