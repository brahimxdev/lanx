info:
  name: 06 reset password
  type: http
  seq: 6

http:
  method: PATCH
  url: http://localhost:3000/api/v1/auth/reset-password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
