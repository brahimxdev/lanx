info:
  name: 08 change email
  type: http
  seq: 8

http:
  method: PATCH
  url: http://localhost:3000/api/v1/auth/change-password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
