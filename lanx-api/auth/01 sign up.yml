info:
  name: 01 sign up
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/sign-up
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
