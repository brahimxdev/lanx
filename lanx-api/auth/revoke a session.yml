info:
  name: revoke a session
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{baseUrl}}/account/sessions/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
