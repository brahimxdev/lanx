info:
  name: Logout User
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/auth/logout"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
