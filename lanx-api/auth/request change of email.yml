info:
  name: request change of email
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/account/email"
  body:
    type: json
    data: |-
      {
        "newEmail": "lekxyyusuf@gmail.com",
        "currentPassword": "IBRahim123!!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
