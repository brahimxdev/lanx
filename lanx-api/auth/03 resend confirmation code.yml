info:
  name: 03 resend confirmation code
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/auth/resend-confirmation-code"
  body:
    type: json
    data: |-
      {
        "email": "ibrahim.techresources@proton.me"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
