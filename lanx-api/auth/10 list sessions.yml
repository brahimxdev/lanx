info:
  name: 10 list sessions
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:3000/api/v1/auth/sessions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
