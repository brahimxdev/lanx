info:
  name: 11 revoke a session
  type: http
  seq: 11

http:
  method: DELETE
  url: http://localhost:3000/api/v1/auth/sessions/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
