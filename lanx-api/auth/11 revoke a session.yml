info:
  name: 11 revoke a session
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{baseUrl}}/auth/sessions/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
