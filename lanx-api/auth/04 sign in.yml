info:
  name: 04 sign in
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/auth/sign-in"
  body:
    type: json
    data: |-
      {
        "email": "ibrahim.techresources@proton.me",
        "password": "IBRahim123!!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
